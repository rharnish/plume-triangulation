# The calibrated camera model: the star-measured fisheye lens and per-camera star poses
# from data/meta/pose_ledger.json. coverage, bias, fig_bearing and terrain_range run under
# this profile unless FIGLIB_PROFILE names another; anything set in the environment wins.
FIGLIB_LENS = "fisheye"
FIGLIB_POSE_LEDGER = "1"
