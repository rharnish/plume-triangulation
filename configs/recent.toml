# The calibrated camera model on the extra CDN cameras (recent.py): the recent corpus's own
# pose ledger, which adds the star solves of cameras FIgLib never annotated.
FIGLIB_LENS = "fisheye"
FIGLIB_POSE_LEDGER = "1"
FIGLIB_POSE_LEDGER_PATH = "out/recent/pose_ledger.json"
